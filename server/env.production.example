# 生产环境配置
# 部署时将此文件复制为 .env 并修改配置

# 数据库 (使用 SQLite)
# 本地部署: file:./prod.db
# Railway 部署 (使用持久化卷): file:/app/data/prod.db
DATABASE_URL="file:./prod.db"

# JWT 密钥 (请更换为随机字符串！)
# 生成方法: 
#   Linux/Mac: openssl rand -base64 32
#   Node.js: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
JWT_SECRET="请更换为一个安全的随机字符串"

# 服务器配置
PORT=8080
NODE_ENV=production


