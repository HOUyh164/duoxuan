# DORA - 卡密销售系统

## 📖 项目简介

DORA 是一个卡密销售管理系统，包含：
- 🌐 用户前台网站
- 🔧 管理员后台
- ⚙️ API 服务

## 🛠️ 技术栈

- **后端**: Node.js + Express
- **数据库**: SQLite + Prisma ORM
- **前端**: HTML + CSS + JavaScript

---

## 🚀 快速开始

### 本地开发

```bash
# 1. 进入服务器目录
cd server

# 2. 安装依赖
npm install

# 3. 初始化数据库
npm run db:generate
npm run db:push

# 4. 启动开发服务器
npm run dev
```

访问：
- 主网站：http://localhost:8080
- 管理后台：http://localhost:8080/admin

---

## ☁️ 部署

### 方式一：Render（推荐）

详见 [DEPLOY-RENDER.md](./DEPLOY-RENDER.md)

**快速部署步骤：**
1. 推送代码到 GitHub
2. 在 [render.com](https://render.com) 创建 Web Service
3. 连接 GitHub 仓库
4. 配置：
   - Root Directory: `server`
   - Build Command: `npm install && npm run build`
   - Start Command: `npm start`
5. 添加环境变量（见下方）

### 方式二：Railway

详见 [DEPLOY-RAILWAY.md](./DEPLOY-RAILWAY.md)

### 方式三：自有服务器

- Linux 部署：[DEPLOY.md](./DEPLOY.md)
- Windows 部署：[DEPLOY-WINDOWS.md](./DEPLOY-WINDOWS.md)

---

## 🔐 环境变量

| 变量名 | 说明 | 示例值 |
|--------|------|--------|
| `DATABASE_URL` | 数据库连接 | `file:./prod.db` |
| `JWT_SECRET` | JWT 密钥 | 随机字符串 |
| `NODE_ENV` | 运行环境 | `production` |
| `PORT` | 服务端口 | `10000` |
| `ADMIN_EMAIL` | 管理员邮箱（可选） | `admin@example.com` |
| `ADMIN_PASSWORD` | 管理员密码（可选） | `yourpassword` |

---

## 👤 默认管理员账户

服务启动时会自动创建管理员账户：

| 字段 | 值 |
|------|-----|
| 邮箱 | `dora@gmail.com` |
| 密码 | `doraai` |

⚠️ **请登录后立即修改密码！**

---

## 📁 项目结构

```
vlore/
├── admin/              # 管理后台
│   ├── css/
│   ├── js/
│   └── index.html
├── server/             # 后端服务
│   ├── src/
│   │   ├── config/     # 配置
│   │   ├── middleware/ # 中间件
│   │   ├── routes/     # API 路由
│   │   └── index.js    # 入口文件
│   ├── prisma/         # 数据库模型
│   ├── package.json
│   ├── railway.json    # Railway 配置
│   └── render.yaml     # Render 配置
├── website/            # 用户前台
│   ├── css/
│   ├── js/
│   └── index.html
└── nginx.conf          # Nginx 配置（自部署用）
```

---

## 🔌 API 端点

### 认证 `/api/auth`

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/register` | 用户注册 |
| POST | `/login` | 用户登录 |
| GET | `/me` | 获取当前用户信息 |

### 卡密 `/api/cards`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/` | 获取卡密列表 |
| POST | `/generate` | 生成卡密（管理员） |

### 订单 `/api/orders`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/` | 获取订单列表 |
| POST | `/` | 创建订单 |

### 管理 `/api/admin`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/stats` | 获取统计数据 |
| GET | `/users` | 获取用户列表 |
| PUT | `/users/:id/role` | 更新用户角色 |
| POST | `/init` | 初始化管理员 |
| GET | `/check-init` | 检查初始化状态 |

### 其他

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/health` | 健康检查 |

---

## 💰 方案定价

| 方案 | 价格 (NT$) | 有效期 |
|------|-----------|--------|
| 日卡 | 即将推出 | 1 天 |
| 周卡 | 即将推出 | 7 天 |
| 月卡 | 1,400 | 30 天 |
| 终身卡 | 8,000 | 永久 |

---

## ❓ 常见问题

### Q: 忘记管理员密码怎么办？
重新部署服务会自动重新创建管理员账户（Render 免费层）。

### Q: 数据丢失了？
Render 免费层不保证数据持久化，建议升级到付费版或改用 PostgreSQL。

### Q: 如何修改默认管理员账户？
修改 `server/src/index.js` 中的 `ADMIN_EMAIL` 和 `ADMIN_PASSWORD`。

---

## 📄 License

MIT License

---

## 🔗 相关链接

- [Render 文档](https://render.com/docs)
- [Railway 文档](https://docs.railway.app/)
- [Prisma 文档](https://www.prisma.io/docs)
- [Express 文档](https://expressjs.com/)
